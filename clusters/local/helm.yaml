apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: cp-helm
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/bcit-ltc/cp-helm-chart
  ref:
    branch: main
  ignore: |
    # exclude all
    /*
    # include charts directory
    !/mychart/ 